import type { ExpensesData } from "./expenses.data.types";
import { MOCK_EXPENSE_ENTRIES } from "@/lib/spaces/expenses/expenses.mock";

export const initialExpensesData: ExpensesData = {
  entries: MOCK_EXPENSE_ENTRIES,
};
